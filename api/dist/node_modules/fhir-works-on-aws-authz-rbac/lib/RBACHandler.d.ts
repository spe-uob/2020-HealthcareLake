import { Authorization, AuthorizationRequest, AuthorizationBundleRequest, AllowedResourceTypesForOperationRequest } from 'fhir-works-on-aws-interface';
import { RBACConfig } from './RBACConfig';
export declare class RBACHandler implements Authorization {
    private readonly version;
    private readonly rules;
    constructor(rules: RBACConfig);
    isAuthorized(request: AuthorizationRequest): Promise<boolean>;
    isBundleRequestAuthorized(request: AuthorizationBundleRequest): Promise<boolean>;
    getAllowedResourceTypesForOperation(request: AllowedResourceTypesForOperationRequest): Promise<string[]>;
    private isAllowed;
}
